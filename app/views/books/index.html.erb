<section class="book_index_page">
  <div class= "row show-grid">
    <div class="row">
        <% @books.each do |book| %>
        <div class="col-xs-6 col-sm-4 col-md-3">
          <%= link_to book do %>
          <div class="thumbnail">
            <%= image_tag book.cover_picture, class: 'book_image' %>
            <div class="caption">
              <div class="category_thumbnail_caption">
                <h5><%= book.name %></h5>
                <h5><%= book.user_id %></h5>
                <h5>£<%= book.price %></h5>
                <h5><%= book.star_rating %></h5>
              </div>
            </div>
          </div>
          <% end %>
        </div>
        <% end %>
    </div>
  </div>
</section>


